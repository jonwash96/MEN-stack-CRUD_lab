<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', { title:`${data.name} ${data.resourceType} Details`}) %>
<body>
    <%- include('../partials/navbar') %>
    
    <h1><% data.name %></h1>
    <ul>
    <% for (let item in data) { %>
        <li>
            <h4><%= item %></h4>
            <p><%= data[item] %></p>
        </li>
    <% } %>
    </ul>
</body>
</html>